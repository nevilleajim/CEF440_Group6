import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primaryDark = Color(0xFF0A2463);
  static const Color primary = Color(0xFF3E92CC);
  static const Color primaryLight = Color(0xFFD8E9F7);

  // Accent colors
  static const Color accent = Color(0xFF1B98E0);
  static const Color accentLight = Color(0xFFE1F2FE);

  // Neutral colors
  static const Color background = Color(0xFFF8FAFD);
  static const Color surface = Colors.white;
  static const Color surfaceVariant = Color(0xFFF1F5FB);

  // Text colors
  static const Color textPrimary = Color(0xFF172B4D);
  static const Color textSecondary = Color(0xFF6B778C);
  static const Color textHint = Color(0xFF97A0AF);

  // Status colors
  static const Color success = Color(0xFF36B37E);
  static const Color warning = Color(0xFFFFAB00);
  static const Color error = Color(0xFFFF5630);
  static const Color info = Color(0xFF00B8D9);

  // Gradient colors
  static const List<Color> loginGradient = [
    Color(0xFF1A3A6C),
    Color(0xFF0F2952),
  ];
}